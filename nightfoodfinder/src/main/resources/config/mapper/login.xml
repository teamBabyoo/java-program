<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nff.repository.dao.LoginDAO">

	<!-- 일반회원 가입(naver) -->
	<insert id="insertNaverUser" parameterType="nUser" >
		insert into tb_naveruser (user_id, user_name, user_email, user_gender, user_age) values
			(#{userId},#{userName},#{userEmail},#{userGender},#{userAge})
	</insert>
	<!-- naver 회원 체크 -->
	<select id="selectNaver" resultType="int" parameterType="nUser">
		select count(*) from tb_naveruser 
			where user_email = #{userEmail}
	</select>
	
	<!-- 가게 이메일 중복체크 -->
	<select id="storeEmailChk" parameterType="String" resultType="int" flushCache="true" useCache="true">
		select count(*) from tb_store where store_email = #{storeEmail}
	</select>
	
	<select id="selectUserInfoByName" parameterType="String" resultType="Store" flushCache="true" useCache="true">
		
	</select>
	
	
</mapper>  